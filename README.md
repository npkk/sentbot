# SentBot

Discordの文章から感情分析を行い、必要な時に低速モードへ移行するBotです。

## 機能
- Discordの文章を監視し、感情分析を実行
- 感情が一定の閾値を超えた場合、チャンネルを低速モードに設定
- スラッシュコマンドによる設定変更

## 開発環境
- Python 3.12
- uv
- Docker

## セットアップ
1. `.env.example` を `.env` にコピーし、必要事項を記入してください。
2. `uv sync` で依存関係をインストールします。
3. `uv run python main.py` で起動します。
